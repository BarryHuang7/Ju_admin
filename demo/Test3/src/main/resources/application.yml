server:
  port: 7777
  tomcat:
    uri-encoding: utf-8
    # 设定tomcat的basedir目录，如果没有指定则使用临时目录
    basedir: /home/app/tomcat_upload_temp

# 配置数据源
spring:
  http:
    encoding:
      charset: utf-8
  application:
    name: demo
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/java_test?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: aced0f8e04167979
# redis
  redis:
    database: 0
    host: 110.41.16.194
    port: 6379
    timeout: 3000
    password: 151417redis
  servlet:
    # 上传限定参数
    multipart:
      # 是否启用分段上传支持
      enabled: true
      # 最大请求大小
      max-request-size: 20MB
      # 最大文件的大小
      max-file-size: 20MB
      # 上传文件的中间位置
      location: /home/app/tomcat_upload_temp

# 指定映射文件的路径
mybatis:
  mapper-locations: classpath:mapper/*.xml

# 微服务
eureka:
  instance:
    prefer-ip-address: true
    ip-address: 110.41.16.194
    port: 9999
  client:
    serviceUrl:
      defaultZone: http://admin:151417eureka@${eureka.instance.ip-address}:${eureka.instance.port}/eureka/

student:
  a: "学生a"